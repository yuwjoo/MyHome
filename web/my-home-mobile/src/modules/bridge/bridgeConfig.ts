/**
 * bridge配置
 */

export const bridgeConfig = {
    nativeInterfaceKey: '__native-interface__',
    commCenterKey: '__webBridge',
    listenerGroup: '__listeners',
    defaultTimeout: 60,
    timeoutMessage: 'timeout',
}