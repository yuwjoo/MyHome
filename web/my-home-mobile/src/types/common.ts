// ── 通用枚举 & 布局 ────────────────────────────────────────────────────────────

/** 底部导航 Tab */
export type NavTab = 'home' | 'express' | 'cloud' | 'profile'

/** 文件列表布局模式 */
export type LayoutMode = 'list' | 'grid'
